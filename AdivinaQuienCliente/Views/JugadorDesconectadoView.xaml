<UserControl x:Class="AdivinaQuienCliente.Views.JugadorDesconectadoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdivinaQuienCliente.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="White" 
     Width="300" Height="150" 
     CornerRadius="15" 
     BorderBrush="#EE1515" 
     BorderThickness="3">

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Se perdio la conexión" 
                Foreground="#333"
                FontSize="20" 
                FontWeight="Bold" 
                HorizontalAlignment="Center"/>

            <TextBlock x:Name="txtMensaje" 
                
                Foreground="#EE1515" 
                FontSize="14" 
                Margin="0,10,0,15" 
                FontWeight="Bold"
                HorizontalAlignment="Center"
            >
                <Run Text="El jugador"></Run>
                <Run Text="{Binding NombreServidor}"></Run>
                <Run Text="se ha desconectado"></Run>
            </TextBlock>

            <Button Command="{Binding VolverInicioCommand}" Content="Regresar" 
             Width="140" Height="30" 
             Background="#EE1515" 
             Foreground="White" 
             FontWeight="Bold"
             Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="10"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Border>
</UserControl>
