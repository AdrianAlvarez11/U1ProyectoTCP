<UserControl x:Class="AdivinaQuienServidor.Views.JuegoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AdivinaQuienServidor.Views"
        mc:Ignorable="d"
        Height="500" Width="850">
    <UserControl.Background>
        <ImageBrush ImageSource="https://wallpapers.com/images/hd/colorful-pokemon-anime-hd-12w1bgtvd51z5zrt.jpg" 
                    Stretch="UniformToFill" 
                    Opacity="0.15"/>
    </UserControl.Background>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#EE1515" Padding="8" CornerRadius="8">
            <DockPanel>
                
                <!--<TextBlock Text="Estado: No iniciado" Foreground="#FFDE00" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,15,0">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="2" ShadowDepth="1"/>
                    </TextBlock.Effect>
                </TextBlock>-->
                
                <TextBlock Text="Turno: " Foreground="White" FontSize="18" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding Juego.JugadorTurno.Nombre}" Foreground="White" FontSize="18" VerticalAlignment="Center"/>

                <Border Background="#CC0000" CornerRadius="15" Padding="15,2" HorizontalAlignment="Right">
                    <TextBlock HorizontalAlignment="Right" Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center">
                        <Run Text="Ronda: "></Run>
                        <Run Text="{Binding Juego.Ronda}"></Run>
                    </TextBlock>
                </Border>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2.5*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">

                <ItemsControl ItemsSource="{Binding Pokemons}" Grid.Row="1" VerticalAlignment="Top">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" Margin="5" Padding="8" CornerRadius="12" BorderBrush="#3B4CCA" BorderThickness="2" Height="150" Width="100">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <Border Grid.Row="0" Background="#F2F2F2" CornerRadius="8">
                                        <Image Source="{Binding Imagen}" Stretch="Uniform"/>
                                    </Border>

                                    <TextBlock Grid.Row="1" Text="{Binding Nombre}" Foreground="#333" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,5" FontSize="12"/>

                                    <Button Grid.Row="2" Content="Descartar" Height="24" Background="#EE1515" Foreground="White" FontSize="10" FontWeight="Bold">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="5"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>


                    </ItemsControl.ItemTemplate>

                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"></WrapPanel>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>

        
            </ScrollViewer>

            <Border Grid.Column="1" Background="White" Margin="10,0,0,0" Padding="12" CornerRadius="12" BorderBrush="#CCC" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0">
                        <Label Content="TU PERSONAJE" HorizontalAlignment="Center" FontSize="14" FontWeight="Bold" Foreground="#3B4CCA" />
                        <Border Height="70" Width="70" Background="#F2F2F2" CornerRadius="8">
                            <Image Source="{Binding MiPokemon.Imagen}" Stretch="Uniform"/>
                        </Border>
                        <TextBlock Grid.Row="1" Text="{Binding MiPokemon.Nombre}" Foreground="#333" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,5" FontSize="15"/>
                    </StackPanel>
                    <StackPanel Grid.Row="1">
                        <TextBlock Text="Historial" Foreground="#3B4CCA" FontSize="12" FontWeight="Bold" Margin="0,0,0,5"/>
                        <ListBox ItemsSource="{Binding Juego.Historial}" Height="100" Background="#F9F9F9" BorderBrush="#EEE" BorderThickness="1" Margin="0,0,0,10"/>


                    </StackPanel>




                    <Grid Grid.Row="2">

                        <!--cuando es mi turno, controles de pregunta-->

                        <Grid>
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Collapsed"></Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding EsMiTurno}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="TU PREGUNTA" Foreground="#333" FontSize="11" FontWeight="Bold" Margin="0,0,0,5"></TextBlock>

                            <TextBox Grid.Row="1" Text="{Binding Juego.Pregunta}" Height="20" Background="#F2F2F2" BorderBrush="#3B4CCA" BorderThickness="1" VerticalContentAlignment="Center" Padding="5,0" Margin="0,0,0,10"></TextBox>

                            <TextBlock Grid.Row="2" Text="{Binding Mensaje}" Foreground="#333" FontSize="11" Margin="0,0,0,0" TextAlignment="Center"></TextBlock>

                            <Button Command="{Binding EnviarPreguntaCommand}" Grid.Row="3" Content="ENVIAR" Height="25" Background="#3B4CCA" FontWeight="Bold" FontSize="14" Margin="0,0,0,5">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="IsEnabled" Value="True"></Setter>
                                        <Setter Property="Foreground" Value="White"></Setter>

                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding EsperandoRespuesta}" Value="True">
                                                <Setter Property="IsEnabled" Value="False"></Setter>
                                                <Setter Property="Foreground" Value="#3B4CCA"></Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="8"></Setter>
                                    </Style>
                                </Button.Resources>
                            </Button>

                            <Button Grid.Row="4" Content="¡ADIVINAR!" Height="25" Background="#FFDE00" Foreground="#3B4CCA" FontWeight="Bold" FontSize="14">

                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="IsEnabled" Value="True"></Setter>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding EsperandoRespuesta}" Value="True">
                                                <Setter Property="IsEnabled" Value="False"></Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="8"></Setter>
                                    </Style>
                                </Button.Resources>
                            </Button>

                        </Grid>


                        <!--cuando es turno del otro, controles de respuesta-->
                        <Grid>
                            
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Visibility" Value="Visible"></Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding EsMiTurno}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"></Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                                <RowDefinition Height="*"></RowDefinition>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="Pregunta:" Foreground="#333" FontSize="11" FontWeight="Bold" Margin="0,0,0,5"></TextBlock>

                            <TextBlock Grid.Row="1" Text="{Binding Juego.Pregunta}" Height="20" Background="#F2F2F2" Padding="5,0" Margin="0,0,0,10"></TextBlock>
                            <TextBlock Grid.Row="2" Text="{Binding Mensaje}" Foreground="#333" FontSize="11" Margin="0,0,0,0" TextAlignment="Center"></TextBlock>

                            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">

                                <Button Command="{Binding EnviarRespuestaCommand}" CommandParameter="SI" Content="SI" Height="25" Width="80" Background="#3B4CCA" FontWeight="Bold" FontSize="14">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="IsEnabled" Value="True"></Setter>
                                            <Setter Property="Foreground" Value="White"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding EsperandoPregunta}" Value="True">
                                                    <Setter Property="IsEnabled" Value="False"></Setter>
                                                    <Setter Property="Foreground" Value="#3B4CCA"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="8"></Setter>
                                        </Style>
                                    </Button.Resources>
                                </Button>

                                <Button Command="{Binding EnviarRespuestaCommand}" CommandParameter="NO" Content="NO" Width="80" Height="25" Background="#FFDE00" Foreground="#3B4CCA" FontWeight="Bold" FontSize="14">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="IsEnabled" Value="True"></Setter>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding EsperandoPregunta}" Value="True">
                                                    <Setter Property="IsEnabled" Value="False"></Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="8"></Setter>
                                        </Style>
                                    </Button.Resources>
                                </Button>

                            </StackPanel>

                        </Grid>

                    </Grid>

                </Grid>
            </Border>

        </Grid>
    </Grid>
</UserControl>
